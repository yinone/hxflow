{
  "feature": "{{feature}}",
  "requirementDoc": "{{requirementDoc}}",
  "planDoc": "{{planDoc}}",
  "createdAt": "{{createdAt}}",
  "updatedAt": "{{updatedAt}}",
  "completedAt": null,
  "lastRun": null,
  "tasks": [
    {
      "id": "{{task.id}}",
      "name": "{{task.name}}",
      "status": "pending",
      "dependsOn": [],
      "parallelizable": false,
      "output": "",
      "startedAt": null,
      "completedAt": null,
      "durationSeconds": null
    }
  ]
}
